These are created using model compression techniques like [[Pruning]], [[Parameter Sharing]], and [[Distillation]] to reduce model size while maintaining accuracy.

- Pruned and optimized architecture
- Memory-efficient, Can run on edge devices
- Complex compression pipeline, [[Fine-tuning]] may be needed
