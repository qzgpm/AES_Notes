These models reduce the precision of weights and activations (e.g., from 32-bit floats to 8-bit integers) to make them smaller and faster.

- Memory-efficient, Lower precision
- Low storage requirement, Speedup during inference
- Can lose numerical precision, May impact accuracy